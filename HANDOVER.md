# HANDOVER.md

- 最終更新: 2026-08-14

## プロジェクト概要

いろいろなAIについて調べた記事をカテゴリ別に載せていく静的サイト「AI調査ノート」。ビルド不要、`index.html` をブラウザで開くだけで動く。

## 今回やったこと

- サイト基盤をゼロから構築
  - `index.html` … トップページ(カテゴリ一覧・記事一覧・検索・カテゴリ絞り込み)
  - `article.html` + `js/article.js` … 記事詳細ページ(`?id=記事ID`、関連記事表示つき)
  - `js/data.js` … 記事・カテゴリのデータファイル(記事追加はここだけ編集すればよい設計)
  - `css/style.css` … デザイン(スマホ対応済み)
  - `README.md` … 記事・カテゴリの追加手順
- 初期カテゴリ9種を定義(ChatGPT / Claude / Gemini / 画像生成 / 動画・音声 / AIコーディング / ローカルLLM / ニュース / ノウハウ)
- サンプル記事2件を登録(サイト開設の挨拶、記事追加方法のメモ)

## 未完了・途中の作業

- なし(基盤は完成・動作確認済み)

## 次にやること(優先順)

1. 実際のAI調査記事を `js/data.js` の `ARTICLES` 先頭に追加していく
2. 記事が増えてきたらページネーション(1ページあたり件数制限)を検討
3. 必要ならカテゴリを `CATEGORIES` に追加

## ハマりポイント・注意点

- 記事の `id` は日付+連番(例: `20260815-01`)。重複するとその記事が開けなくなる
- 記事の `category` は `CATEGORIES` の id と一致させること(不一致だとバッジが表示されない)
- 本文 `content` はバッククォート(`` ` ``)で囲んだHTML。本文中にバッククォートを書くとエラーになるので注意
- git はまだ初期化していない(必要になったら `git init` する)
